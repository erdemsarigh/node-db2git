/ -- son satır
  CREATE TABLE "MS"."MAILLOG" 
   (	"LOGID" NUMBER(10,0) NOT NULL ENABLE, 
	"CREATEDATE" DATE DEFAULT SYSDATE, 
	"MAILTEMPLATEID" NUMBER(4,0), 
	"MAILBODY" CLOB, 
	"MAILSUBJECT" VARCHAR2(1000), 
	"FROMADDRESS" VARCHAR2(500), 
	"MAILTO" VARCHAR2(500), 
	"MAILCC" VARCHAR2(500), 
	"MAILBCC" VARCHAR2(500), 
	"FROMNAME" VARCHAR2(500), 
	"REPLYADDRESS" VARCHAR2(500), 
	"ERRORMESSAGE" VARCHAR2(500)
   ) ;